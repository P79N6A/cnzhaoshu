<?php  



	// ignore_user_abort();//关掉浏览器，PHP脚本也可以继续执行.
	// set_time_limit(0);// 通过set_time_limit(0)可以让程序无限制的执行下去
	// $interval=60*60;// 每隔1小时运行
	// do{
	//   $run = include 'timerstop.php';
	//   if(!$run) die;
	//   $date = rand(1000000,999999999);
	//   file_put_contents("work.txt",$date);

	//   sleep($interval);// 等待5分钟
	// }
	// while(true);
$time=3;
$url="http://test.cnzhaoshu.com/com/dingshiqi.php";

  $run = include '22.php';
  if(!$run) die;
  $date = rand(1000000,999999999);
  file_put_contents("work.txt",$date);

sleep($time);
file_get_contents($url);